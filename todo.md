# TODO

Замечания по методике Simple Spec (plan = понимание задачи, code = истина).

## Ошибки концепта

- [ ] **Переименование плана**: протокол смены имени файла и миграции временных
  меток `// plan:` в тестах (пока файл ещё в `plan/`).
- [ ] **Синтаксис ссылки привязан к C-подобным языкам**: определить формат как
  «комментарий, содержащий `plan: <имя>`», конкретный синтаксис — забота
  `Verification` уровня. Заодно решить: может ли один тест ссылаться на два
  плана (тест взаимодействия).
- [ ] **Языковая политика не зафиксирована**: объявить заголовки шаблона
  (`## Поведение`, `## Состояния`, …) каноническими; задать конвенцию имени
  файла (язык, kebab-case).
- [ ] **Скрипты непортабельны (GNU-измы)**: `find -printf` в
  `check-plan-names.sh` и `\b` в sed `install.sh` — на macOS ломаются молча
  (ссылки на README не перепишутся, проверка имён упадёт).

## Несостыковки документации

- [ ] **Монорепо**: несколько корней `plan/` скрипт допускает (уникальность —
  внутри каждого), но область поиска `// plan:` и разрешение одинаковых имён
  между корнями не определены.

## Добавить

- [ ] **Детерминированная проверка меток** (расширить скрипт, годится в CI):
  висячие `// plan:` на отсутствующие в `plan/` файлы.
- [ ] **Обратный поток при живом плане**: реализация обнаружила поведение,
  которого нет в плане (краевой случай), — сначала дописать план, потом тест.
- [ ] **Место для нефункциональных и кросс-сущностных требований** («отклик не
  дольше секунды», «работает офлайн»): кандидат — `Non-Obvious Rules` /
  `Verification` уровня. Сказать явно, чтобы не загрязняли язык планов.
- [ ] **Маркер версии кита** при установке — повторный `install.sh` обновляет
  файлы молча; в проекте должно быть видно, какой версии методики соответствует
  подложка.
