# Simple Spec

Набор команд для ИИ-агента (opencode, Claude Code, Cursor). Понимание задачи
фиксируют **историей**, историю реализуют **кодом**, а спека сделанного живёт в
именах тестов. Цикл из двух команд: `/story` → `/start`.

## Модель

[story] → [code]

- **Story** — файл в `stories/`: голос пользователя (что нужно и зачем) и его
  дистилляция (пункты поведения). Это **append-only лог намерений**, как
  changelog: старые записи не переписывают под новый код.
- **Code** — реализация и источник истины. Имена тестов на языке пользователя
  повторяют пункты поведения истории — это и есть спека сделанного.

Стрелка `→` — проекция: пункты поведения истории становятся именами тестов. Она
переживает устаревание записи: лог остаётся записью прошлого намерения, имена
тестов продолжают говорить, что сделано.

Вторая опора модели — **уровень**: `AGENTS.md` в каждой значимой папке
иерархии. Запись говорит, *что* должен увидеть пользователь; уровень говорит,
*как это проверяется* (раздел `Verification`) и какие правила нельзя вывести из
кода (`Non-Obvious Rules`). Без уровня нельзя понять, реализована ли фича.

## Цикл истории

```
main ──●──────────────────────────────────●── merge: только код и тесты
        \                                  /
         ●── история ──●── код + тесты ──●
       /story       /start            PR: текст истории в теле
```

`/story` — снять голос пользователя, зафиксировать понимание задачи и записать
историю в `stories/` одним файлом. `/start` — реализация истории: создаёт ветку
от главной (имя ветки = имя истории), коммитит историю первым коммитом, читает её
как единственный источник понимания, пишет код и тесты по пунктам
`## Поведение` и открывает PR.

Мелочь можно вести без истории: ветка, `/start` сразу.

`/architect` — вне цикла: скелет приложения, то есть файловая иерархия с
`AGENTS.md` на каждом уровне (или готовый шаблон из `src/templates/`). Растёт по
надобности, не наперёд.

## Почему так

Постоянная спека, которую держат рядом с кодом вечно, рано или поздно перестают
обновлять — и тогда врут оба, а верить приходится коду. Simple Spec не даёт
такой спеке появиться — и не выбрасывает намерение. Проверяемым остаётся то, что
исполняется, — тесты; намерение и понимание задачи — в истории (`stories/`);
решения, которые нельзя вывести из кода, — в «почему»-комментариях (самое
ценное — отвергнутая альтернатива) и в `Non-Obvious Rules` уровня; отмена
прежнего поведения — в сообщении коммита, удалившего тест.

Чем это не является: не трекер задач, не документация архитектуры (её место —
`AGENTS.md` уровня), не BDD-фреймворк — какими видами тестов проверять, решает
уровень.

## Что где лежит

- `src/commands/story.md` — история: команда и формат записи в `stories/`.
- `src/commands/start.md` — команда реализации истории.
- `src/commands/architect.md` — команда построения скелета.
- `src/AGENTS.md` — формат уровня: границы, паттерны, `Verification`.
- `src/templates/` — готовые скелеты архитектур для `/architect`
  (например `web-app/`, образец — `pg`, `fitness`).
- `src/commands/` — сами команды.

## Установка

Команды и кит (`AGENTS.md`) ставит `install.sh` — в проект или глобально для
пользователя:

```
./install.sh <путь-к-проекту> [служебная-папка]   # по умолчанию .opencode
./install.sh --global [opencode|claude|cursor]    # глобально для юзера
```

Кит едет рядом с командами, ссылки внутри команд переписываются на его путь.
Повторный запуск обновляет кит и не трогает `stories/` проекта. Подробности — в
шапке `install.sh`.