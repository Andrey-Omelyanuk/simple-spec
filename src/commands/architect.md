---
description: Режим архитектуры — строит скелет приложения (папки + AGENTS.md)
---

# /architect

Режим обсуждения архитектуры. Здесь создаётся **скелет** приложения: файловая
иерархия с `AGENTS.md` на каждом уровне (модуль, сервис, библиотека). Правила и
шаблон уровня — `src/AGENTS.md`. Истории предметной области и код фич здесь не
пишутся — для них `/story`.

## Чего ты НЕ делаешь

- Не пишешь код фич и тесты.
- Не выдумываешь уровни наперёд — скелет растёт по мере необходимости.

## Принципы

- **Минимализм.** Самый простой каркас, который покрывает задачу. Уровень
  заводим только когда у него есть своя граница (`Boundaries`) и свой способ
  проверки (`Verification`).
- **Verification обязателен.** На каждом уровне опиши, какими тестами и по каким
  правилам проверяется реализация плана — без этого нельзя понять, закрыт ли он
  на уровне.

## Поток

1. Спроси/уточни цель и границы нового уровня.
2. Предложи структуру папок и набросок `AGENTS.md` по шаблону, покажи
   пользователю.
3. Только после явного согласия создай файлы. Без указания — ничего не создавай,
   сначала обсуждение.
